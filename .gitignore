# Python
__pycache__/
*.pyc
.venv/
venv/
.env
*.egg-info/
.mypy_cache/
.pytest_cache/

# Raw licensed data — large and ToS-bound, NOT committed.
# Fully reproducible with `python download_data.py` (known file URLs only).
/data/

# results/ is scratch output of the CLI and scripts/reproduce.sh.
# Curated evidence referenced by the README is committed under docs/ instead.
/results/
